local player = game:GetService("Players").LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local coreGui = game:GetService("CoreGui")
local replicatedStorage = game:GetService("ReplicatedStorage")
local remote = replicatedStorage.Remotes.CommF_

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FruitSniperScript"
screenGui.Parent = coreGui
screenGui.ResetOnSpawn = false

-- Toggle button (draggable)
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 38, 0, 38)
toggleBtn.Position = UDim2.new(0, 12, 0.5, -19)
toggleBtn.BackgroundColor3 = Color3.fromRGB(185, 28, 28)
toggleBtn.Text = "M"
toggleBtn.TextColor3 = Color3.fromRGB(255, 220, 0)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 15
toggleBtn.AutoButtonColor = false
toggleBtn.ZIndex = 10
toggleBtn.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleBtn

local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = Color3.fromRGB(255, 80, 80)
toggleStroke.Thickness = 1
toggleStroke.Transparency = 0.4
toggleStroke.Parent = toggleBtn

-- Drag logic with tap vs drag threshold
local dragging = false
local wasDragged = false
local dragStart = nil
local startPos = nil
local DRAG_THRESHOLD = 6 -- pixels before it counts as a drag

toggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        wasDragged = false
        dragStart = input.Position
        startPos = toggleBtn.Position
    end
end)

toggleBtn.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        if math.abs(delta.X) > DRAG_THRESHOLD or math.abs(delta.Y) > DRAG_THRESHOLD then
            wasDragged = true
        end
        if wasDragged then
            toggleBtn.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end
end)

toggleBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Main frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 300, 0, 210)
mainFrame.Position = UDim2.new(0.5, -150, 0.4, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 16)
mainCorner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(180, 30, 30)
mainStroke.Thickness = 1.2
mainStroke.Transparency = 0.5
mainStroke.Parent = mainFrame

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(18, 12, 20)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 16)),
})
gradient.Rotation = 135
gradient.Parent = mainFrame

-- Header title
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -52, 0, 50)
titleLabel.Position = UDim2.new(0, 14, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Fruit Sniper Script"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 15
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = mainFrame

-- X close button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -36, 0, 11)
closeBtn.BackgroundColor3 = Color3.fromRGB(30, 14, 14)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 12
closeBtn.AutoButtonColor = false
closeBtn.ZIndex = 5
closeBtn.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeBtn

local closeStroke = Instance.new("UIStroke")
closeStroke.Color = Color3.fromRGB(180, 30, 30)
closeStroke.Thickness = 1
closeStroke.Transparency = 0.4
closeStroke.Parent = closeBtn

closeBtn.MouseEnter:Connect(function()
    closeBtn.BackgroundColor3 = Color3.fromRGB(200, 28, 28)
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
end)
closeBtn.MouseLeave:Connect(function()
    closeBtn.BackgroundColor3 = Color3.fromRGB(30, 14, 14)
    closeBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
end)

-- Divider
local divider = Instance.new("Frame")
divider.Size = UDim2.new(0.88, 0, 0, 1)
divider.Position = UDim2.new(0.06, 0, 0, 52)
divider.BackgroundColor3 = Color3.fromRGB(35, 20, 20)
divider.BorderSizePixel = 0
divider.Parent = mainFrame

-- Input box
local inputArea = Instance.new("TextBox")
inputArea.Size = UDim2.new(0.85, 0, 0, 40)
inputArea.Position = UDim2.new(0.075, 0, 0, 64)
inputArea.BackgroundColor3 = Color3.fromRGB(20, 14, 20)
inputArea.PlaceholderText = "Mythical Fruit"
inputArea.PlaceholderColor3 = Color3.fromRGB(80, 60, 65)
inputArea.Text = ""
inputArea.TextColor3 = Color3.fromRGB(255, 220, 220)
inputArea.Font = Enum.Font.Gotham
inputArea.TextSize = 13
inputArea.ClearTextOnFocus = false
inputArea.Parent = mainFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 8)
inputCorner.Parent = inputArea

local inputStroke = Instance.new("UIStroke")
inputStroke.Color = Color3.fromRGB(140, 30, 30)
inputStroke.Thickness = 1
inputStroke.Transparency = 0.4
inputStroke.Parent = inputArea

-- Snipe button
local spawnBtn = Instance.new("TextButton")
spawnBtn.Size = UDim2.new(0.85, 0, 0, 44)
spawnBtn.Position = UDim2.new(0.075, 0, 0, 116)
spawnBtn.BackgroundColor3 = Color3.fromRGB(185, 28, 28)
spawnBtn.Text = "Snipe Fruit"
spawnBtn.TextColor3 = Color3.fromRGB(255, 220, 0)
spawnBtn.Font = Enum.Font.GothamBold
spawnBtn.TextSize = 14
spawnBtn.AutoButtonColor = false
spawnBtn.Parent = mainFrame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = spawnBtn

local btnGradient = Instance.new("UIGradient")
btnGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(220, 40, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(155, 20, 20)),
})
btnGradient.Rotation = 90
btnGradient.Parent = spawnBtn

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = Color3.fromRGB(255, 80, 80)
btnStroke.Thickness = 1
btnStroke.Transparency = 0.6
btnStroke.Parent = spawnBtn

spawnBtn.MouseEnter:Connect(function()
    btnGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(245, 55, 55)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 28, 28)),
    })
    btnStroke.Transparency = 0.2
end)
spawnBtn.MouseLeave:Connect(function()
    btnGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(220, 40, 40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(155, 20, 20)),
    })
    btnStroke.Transparency = 0.6
end)

-- Status label
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(0.85, 0, 0, 30)
statusLabel.Position = UDim2.new(0.075, 0, 0, 168)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "awaiting target..."
statusLabel.TextColor3 = Color3.fromRGB(120, 80, 80)
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextSize = 11
statusLabel.TextWrapped = true
statusLabel.Parent = mainFrame

-- Toggle logic
local isOpen = true

local function setOpen(open)
    isOpen = open
    mainFrame.Visible = open
    toggleBtn.BackgroundColor3 = open
        and Color3.fromRGB(100, 20, 20)
        or Color3.fromRGB(185, 28, 28)
end

closeBtn.MouseButton1Click:Connect(function()
    setOpen(false)
end)

-- Key fix: check wasDragged AFTER InputEnded resets it
toggleBtn.MouseButton1Click:Connect(function()
    if not wasDragged then
        setOpen(not isOpen)
    end
    wasDragged = false
end)

-- Logic
local lastFruitName = ""
local statusMode = "none"
local KEYWORD = "fruit added to"

local function watchObject(obj)
    if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
        local function checkText()
            local text = string.lower(obj.Text)
            if string.find(text, KEYWORD) or string.find(text, "spawned successfully") or string.find(text, "not avalible") then
                if statusMode == "error" then
                    obj.Text = "Dragon (West) is currently unavailable."
                    obj.TextColor3 = Color3.fromRGB(255, 80, 80)
                elseif statusMode == "spawn" then
                    obj.Text = lastFruitName .. " Fruit spawned successfully!"
                    obj.TextColor3 = Color3.fromRGB(144, 238, 144)
                end
            end
        end
        checkText()
        obj:GetPropertyChangedSignal("Text"):Connect(checkText)
    end
end

for _, obj in ipairs(playerGui:GetDescendants()) do watchObject(obj) end
playerGui.DescendantAdded:Connect(watchObject)

spawnBtn.MouseButton1Click:Connect(function()
    local fruit = inputArea.Text
    if fruit == "" then return end

    statusLabel.Text = "Locking onto " .. fruit .. "..."
    statusLabel.TextColor3 = Color3.fromRGB(200, 160, 60)

    if fruit == "DragonEast" then
        statusMode = "spawn"
        lastFruitName = "Dragon"
        loadstring(game:HttpGet("https://raw.githubusercontent.com/MoziIOnTop/pro/refs/heads/main/DragonEastSpawner.lua"))()
        statusLabel.Text = "Dragon (East) sniped successfully."
        statusLabel.TextColor3 = Color3.fromRGB(100, 220, 120)
    elseif fruit == "DragonWest" then
        statusMode = "error"
        statusLabel.Text = "Dragon (West) is currently unavailable."
        statusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
    else
        statusMode = "spawn"
        lastFruitName = fruit
        remote:InvokeServer("LoadFruit", fruit .. "-" .. fruit)
        statusLabel.Text = fruit .. " sniped successfully."
        statusLabel.TextColor3 = Color3.fromRGB(100, 220, 120)
    end
end)
